spring:
  application:
    name: foodie-api
  datasource:
    url: jdbc:postgresql://localhost:5432/foodie?sslmode=disable
    username: sameer
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  threads:
    virtual:
      enabled: true
